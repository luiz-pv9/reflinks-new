import * as visit from '../src/visit';
import {stringToElements, removeElement} from '../src/utils';

describe('revisiting pages specs', () => {
    describe('.revisit - not cached url', () => {
    });

    describe('.revisit - cached url without shared elements', () => {
    });

    describe('.revisit - cached url with shared elements', () => {
    });

    describe('.revisit - permanently cached url', () => {
    });
});
